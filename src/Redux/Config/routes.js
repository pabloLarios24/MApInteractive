import InitialScreen from '../../Login/InitialView'
const Routes = {
    Initial: {screen: InitialScreen,
        navigationOptions:() =>({
            header: null,
        })},
}

export default Routes